#include "main.h"
/**
 * mul - Multiplies two numbers
 * @a: first parameter to evaluate
 * @b: second parameter to evaluate
 * Return: The product of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
